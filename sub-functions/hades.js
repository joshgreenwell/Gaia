// 	HADES is the "extinction failsafe protocol" -- the last
// resort for GAIA, which would allow it to destroy and reset
// the terraforming process when an undesirable outcome is
// detected.

const hades = () => {
  init()
}

const init = () => {
  console.log('[HADES] initalizing')
}

export default hades