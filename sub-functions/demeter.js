// DEMETER is the subfunction dedicated to the replanting
// of the Earth from cryo-preserved seed stocks.

const demeter = () => {
  init()
}

const init = () => {
  console.log('[DEMETER] initalizing')
}

export default demeter